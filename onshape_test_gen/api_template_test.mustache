package onshape_test

import (
	"context"
	"testing"

	"github.com/stretchr/testify/assert"
	"github.com/onshape-public/go-client/onshape"
)

func initialize{{APIName}}Tests(t *testing.T) TestingContext {
    client, err := onshape.NewAPIClientFromEnv()
    assert.NoError(t, err)

    ctx := context.Background()

    return TestingContext{
		"client":     client,
		"ctx":        ctx,
		"ApiService": client.{{APIName}}Api,
	}
}

func Test{{APIName}}API(t *testing.T) {
    ctx := initialize{{APIName}}Tests(t)

    {{#Requests}}
    OpenAPITest{
        Call: onshape.{{.}}{},
        Expect: Todo,
    }.Execute(ctx, t)
    
    {{/Requests}}
}